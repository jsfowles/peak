import { DefaultTheme } from 'styled-components';

export const breakpoints: DefaultTheme["breakpoints"]  = {
  TABLET: '@media (min-width: 768px)',
  DESKTOP: '@media (min-width: 1200px)',
  DESKTOP_HD: '@media (min-width: 1400px)',
};
